- case project.import_status
- when 'finished'
  = icon('check')
  = _('Done')
- when 'started'
  .spinner.spinner-md
  = _('Started')
- when 'failed'
  = _('Failed')
- else
  = project.human_import_status_name
