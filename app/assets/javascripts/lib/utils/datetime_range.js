/**
 * 
 * @param {*} timeWindow - A relative time window
 */
export const getTimeRange = (timeWindow) => {
  // TODO Implement
  throw new Error();
};


// prevent babel-plugin-rewire from generating an invalid default during karma tests
export default () => {};
