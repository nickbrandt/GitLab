query groupEpics($fullPath: ID!) {
  group(fullPath: $fullPath) {
    epics(first: 10) {
      nodes {
        id
        iid
        title
        state
        webUrl
        descendantCounts {
          openedIssues
          closedIssues
        }
      }
    }
  }
}
