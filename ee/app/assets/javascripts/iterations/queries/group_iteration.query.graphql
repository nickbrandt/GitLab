query GroupIteration($groupPath: ID!) {
  group(fullPath: $groupPath) {
    iterations(last: 1) {
      nodes {
        title
        state
        id
        description
        webPath
        startDate
        dueDate
      }
    }
  }
}
