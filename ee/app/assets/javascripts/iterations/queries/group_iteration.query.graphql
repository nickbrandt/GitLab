query GroupIteration($groupPath: ID!) {
  group(fullPath: $groupPath) {
    iterations(first: 1) {
      nodes {
        title
        state
        id
        description
        webPath
        startDate
        dueDate
      }
    }
  }
}
