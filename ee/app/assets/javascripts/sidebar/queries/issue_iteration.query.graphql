query issueSprint($fullPath: ID!, $iid: String!) {
  project(fullPath: $fullPath) {
    issues (iid: $iid, first: 1) {
      nodes {
        iteration {
          id
          title
        }
      }
    }
  }
}
