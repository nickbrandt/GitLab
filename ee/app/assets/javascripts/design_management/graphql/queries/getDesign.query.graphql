#import "../fragments/design.fragment.graphql"

query project($fullPath: ID!, $iid: String!, $atVersion: ID, $designIds: [ID!]) {
  project(fullPath: $fullPath) {
    id
    issue(iid: $iid) {
      designs {
        designs(atVersion: $atVersion, ids: $designIds) {
          edges {
            node {
              ...DesignItem
            }
          }
        }
      }
    }
  }
}
