- if environment
  %div
    %span.flex-truncate-child
      - if last_deployment
        = "##{last_deployment.iid}"
        = "by " + user_avatar(user: last_deployment.user, size: 20) if last_deployment.user
      - else
        = "-"
- else
  = s_('ProtectedEnvironment|(environment was deleted from repository)')
