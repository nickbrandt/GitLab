require 'spec_helper'

describe 'Group Cycle Analytics' do
  before do
    sign_in(create(:admin))
  end

  context 'as a user with the correct priviledges' do 
    it 'renders the page with the correct empty state' do
  
    end

    describe 'displays correct information when selecting items' do

      it 'hides the empty state after item selection' do
  
      end
  
      it 'displays the activity table with 2 items for group selection' do

      end
  
      it 'displays the activity table with 3 items for projects selection' do

      end
    end
  end
end
