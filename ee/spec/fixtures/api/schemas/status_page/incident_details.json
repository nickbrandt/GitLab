{
  "type": "object",
  "allOf": [
    { "$ref": "basic_incident.json" },
    {
      "required": ["comments", "description"],
      "properties": {
        "type": "object",
        "items": {
          "required": ["note", "created_at"],
          "properties": {
            "note": { "type": "string" },
            "created_at":  { "type": "string", "format": "date-time"}
          },
          "additionalProperties": false
        }
      }
    }
  ]
}
