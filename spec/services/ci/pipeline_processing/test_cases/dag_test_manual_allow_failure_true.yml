config:
  test:
    stage: test
    when: manual
    allow_failure: true
    script: exit 1

  deploy:
    stage: deploy
    script: exit 0
    needs: [test]

init:
  expect:
    pipeline: pending
    stages:
      test: skipped
      deploy: pending
    jobs:
      test: manual
      deploy: pending

transitions:
  - event: enqueue
    jobs: [test]
    expect:
      pipeline: pending
      stages:
        test: pending
        deploy: pending
      jobs:
        test: pending
        deploy: pending

  - event: run
    jobs: [test]
    expect:
      pipeline: running
      stages:
        test: running
        deploy: pending
      jobs:
        test: running
        deploy: pending

  - event: drop
    jobs: [test]
    expect:
      pipeline: running
      stages:
        test: success
        deploy: pending
      jobs:
        test: failed
        deploy: pending

  - event: success
    jobs: [deploy]
    expect:
      pipeline: success
      stages:
        test: success
        deploy: success
      jobs:
        test: failed
        deploy: success
